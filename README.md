# Personal Teaching Bot 🤖

Telegram-бот для записи на занятия с админ-панелью управления курсами и заявками.

## 📋 Описание

Бот предназначен для автоматизации процесса записи учеников на занятия. Позволяет:
- Регистрировать заявки от родителей
- Управлять курсами и их описаниями
- Назначать уроки с датой и ссылкой
- Отменять и переносить занятия
- Вести архив заявок

## 🔧 Последние исправления (v2.1)

### Критические исправления процесса регистрации:

#### ✅ Исправлена валидация имен
- **Проблема:** Имена с заглавными буквами не принимались
- **Решение:** Обновлено регулярное выражение для поддержки заглавных букв
- **Пример:** Теперь "Иван Петров" принимается корректно

#### ✅ Добавлена проверка дублей заявок
- **Проблема:** Пользователи могли создавать несколько заявок
- **Решение:** Проверка существующих заявок перед созданием новой
- **Результат:** Предотвращение дублирования данных

#### ✅ Реализовано сохранение состояния регистрации
- **Проблема:** Данные терялись при перезапуске бота
- **Решение:** Полный переход на StateManager с персистентностью
- **Результат:** Восстановление состояния после перезапуска

#### ✅ Добавлен таймаут регистрации
- **Проблема:** Незавершенные регистрации висели бесконечно
- **Решение:** Автоматическая очистка через 30 минут
- **Результат:** Очистка "зависших" регистраций

#### ✅ Улучшена обработка ошибок
- **Проблема:** Непонятные сообщения об ошибках
- **Решение:** Кастомные исключения с понятными сообщениями
- **Результат:** Лучший UX для пользователей

### Тестирование исправлений:
```bash
# Запуск тестов исправлений
python test_registration_fixes.py

# Запуск всех тестов
python run_tests.py
```

### Новые функции:
- `start_registration(user_id)` - начало регистрации
- `is_registration_in_progress(user_id)` - проверка активной регистрации
- `cleanup_expired_registrations()` - очистка просроченных регистраций
- Кастомные исключения для регистрации

## 🚀 Установка и запуск

### Предварительные требования
- Python 3.8+
- Telegram Bot Token (получить у @BotFather)

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка конфигурации
1. Скопируйте `config.env.example` в `config.env`
2. Заполните необходимые переменные:
   ```
   API_TOKEN=ваш_токен_бота
   ADMIN_ID=ваш_telegram_id
   ```

### Запуск бота
```bash
python bot.py
```

Или используйте bat-файл:
```bash
start.bat
```

## 🧪 Тестирование

### Запуск всех тестов
```bash
python run_tests.py
```

### Запуск отдельных тестов
```bash
# Тесты безопасности
python -m pytest tests/test_security.py -v

# Тесты базы данных
python -m pytest tests/test_database.py -v

# Тесты StateManager
python -m pytest tests/test_state_manager.py -v
```

### Запуск с покрытием кода
```bash
python -m pytest tests/ --cov=. --cov-report=html
```

## 🔒 Безопасность

### Система безопасности включает:

1. **Валидация переменных окружения**
   - Строгая проверка API_TOKEN и ADMIN_ID
   - Автоматическая остановка при отсутствии критических переменных

2. **Rate Limiting**
   - Ограничение количества запросов в минуту
   - Настраиваемые лимиты для разных действий
   - Автоматическая блокировка при превышении

3. **Валидация входных данных**
   - Проверка имен (только буквы, пробелы, дефисы)
   - Валидация возраста (3-100 лет)
   - Проверка формата телефона
   - Санитизация всех входных данных

4. **Система банов**
   - Автоматический бан при множественных нарушениях
   - Логирование всех подозрительных действий
   - Возможность ручного бана администратором

5. **Логирование безопасности**
   - Отдельный файл `security.log`
   - Маскирование чувствительных данных
   - Отчеты по событиям безопасности

6. **Управление состоянием**
   - Thread-safe StateManager
   - Персистентность данных
   - Автоматическое сохранение

7. **Обработка ошибок**
   - Кастомные исключения
   - Детальное логирование
   - Graceful degradation

### Команды безопасности для администратора:
- `/security_report` - Отчет по событиям безопасности за 24 часа

### Настройки безопасности в config.env:
```env
MAX_MESSAGE_LENGTH=1000      # Максимальная длина сообщения
MAX_NAME_LENGTH=50          # Максимальная длина имени
RATE_LIMIT_PER_MINUTE=30    # Лимит запросов в минуту
BAN_THRESHOLD=5             # Количество нарушений до бана
```

## 📁 Структура проекта

```
personal teaching/
├── bot.py                 # Главный файл бота
├── config.py              # Конфигурация
├── config.env             # Переменные окружения
├── requirements.txt       # Зависимости
├── README.md             # Документация
├── run_tests.py          # Скрипт запуска тестов
├── data/
│   ├── db.py             # Работа с базой данных
│   └── database.db       # SQLite база данных
├── handlers/
│   ├── admin.py          # Админ-панель
│   ├── admin_actions.py  # Действия админа
│   ├── commands.py       # Пользовательские команды
│   ├── course_editor.py  # Редактор курсов
│   ├── registration.py   # Регистрация заявок
│   └── utils.py          # Утилиты
├── services/
│   └── monitor.py        # Мониторинг заявок
├── state/
│   ├── users.py          # Состояния пользователей (обратная совместимость)
│   └── state_manager.py  # Новый менеджер состояний
├── tests/                # Тесты
│   ├── test_security.py  # Тесты безопасности
│   ├── test_database.py  # Тесты базы данных
│   └── test_state_manager.py # Тесты StateManager
└── utils/
    ├── menu.py           # Меню бота
    ├── security.py       # Система безопасности
    ├── security_logger.py # Логирование безопасности
    ├── exceptions.py     # Кастомные исключения
    └── middleware.py     # Middleware для безопасности
```

## 🗄️ База данных

### Таблица `applications`
- `id` - Уникальный идентификатор
- `tg_id` - Telegram ID пользователя
- `parent_name` - Имя родителя
- `student_name` - Имя ученика
- `age` - Возраст ученика
- `contact` - Контактная информация
- `course` - Выбранный курс
- `lesson_date` - Дата урока (DATETIME)
- `lesson_link` - Ссылка на урок
- `status` - Статус заявки
- `created_at` - Дата создания (DATETIME)

### Таблица `courses`
- `id` - Уникальный идентификатор
- `name` - Название курса
- `description` - Описание курса
- `active` - Активность курса

### Таблица `archive`
- Архив отмененных заявок

## 🎯 Функциональность

### Для пользователей
- `/start` - Главное меню
- `/my_lesson` - Информация о занятии
- `/help` - Справка
- 📋 Записаться - Регистрация заявки
- 📅 Мое занятие - Просмотр назначенного занятия
- 📚 Доступные курсы - Список курсов

### Для администратора
- 📋 Список заявок - Просмотр активных заявок
- 📚 Редактировать курсы - Управление курсами
- ✅ Завершить заявку - Завершение занятий
- ❌ Отменить заявку - Отмена заявок
- 🚫 Отменить урок - Отмена назначенных уроков
- 🕓 Перенести урок - Перенос уроков
- `/security_report` - Отчет по безопасности

## 🔧 Команды администратора

- `/clear` - Очистка всех заявок
- `/clear_archive` - Очистка архива
- `/security_report` - Отчет по безопасности

## 📝 Логирование

Бот ведет логи в файлы:
- `bot.log` - Основные логи приложения
- `security.log` - Логи событий безопасности

Уровни логирования:
- INFO - Информационные сообщения
- WARNING - Предупреждения
- ERROR - Ошибки
- DEBUG - Отладочная информация

## 🛠️ Разработка

### Добавление новых функций
1. Создайте обработчик в папке `handlers/`
2. Зарегистрируйте его в `bot.py`
3. Добавьте необходимые функции в `data/db.py`
4. Обновите меню в `utils/menu.py`
5. Напишите тесты в папке `tests/`

### Тестирование
```bash
# Тест базы данных
python -c "from data.db import init_db; init_db(); print('DB initialized')"

# Тест конфигурации
python -c "from config import API_TOKEN; print('Config loaded')"

# Тест безопасности
python -c "from utils.security import validate_user_input; print('Security system loaded')"

# Запуск всех тестов
python run_tests.py
```

## 🔒 Безопасность

- API токен хранится в переменных окружения
- Валидация всех входных данных
- Параметризованные SQL-запросы
- Ограничение доступа к админ-функциям
- Rate limiting для защиты от спама
- Thread-safe управление состоянием
- Кастомные исключения для лучшей обработки ошибок

## 🆕 Новые улучшения

### 1. Система тестирования
- Unit тесты для всех основных компонентов
- Тесты безопасности и валидации
- Тесты базы данных с временными файлами
- Тесты StateManager с многопоточностью

### 2. Управление состоянием
- Новый StateManager с thread-safety
- Персистентность данных в JSON
- Автоматическое сохранение
- Обратная совместимость

### 3. Обработка ошибок
- Кастомные исключения для разных типов ошибок
- Детальное логирование с контекстом
- Graceful degradation при ошибках
- Понятные сообщения для пользователей

### 4. Улучшенная безопасность
- Интеграция с StateManager
- Лучшая обработка исключений
- Более детальное логирование
- Thread-safe операции

## 📊 Мониторинг

Бот автоматически проверяет новые заявки каждые 60 секунд и уведомляет пользователей о назначенных занятиях.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в файле `bot.log`
2. Проверьте логи безопасности в `security.log`
3. Убедитесь в правильности конфигурации
4. Проверьте подключение к интернету
5. Обратитесь к администратору

## 📄 Лицензия

Проект разработан для личного использования.

---

**Версия:** 1.0.0  
**Автор:** Никита  
**Дата:** 2024 